# Milestone 8 — GDPR and PII Documentation

Create:

```text
docs/day2_gdpr_pii_handling.md
```

Document:

- which fields are PII
- which fields were hashed
- which fields were removed from Silver
- which fields should not appear in Gold dashboards
- how `gdpr_consent` should be respected
- which roles should access raw PII
- which roles should only access anonymized or aggregated outputs
