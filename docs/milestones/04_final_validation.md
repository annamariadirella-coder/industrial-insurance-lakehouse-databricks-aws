# 04 Final Validation

Run final checks: Gold tables exist, dashboard views return rows, feature table has one row per claim, dashboard views do not expose raw PII, KPI values are reasonable, quarantine evidence exists.
